<resources>

    <!-- Base application theme. -->
    <style name="BaseAppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimaryDark">@color/fresco_orange</item>
        <item name="colorPrimary">@color/fresco_yellow</item>
    </style>

    <!-- Status bar, can do nothing because SDK16  -->
    <style name="AppTheme" parent="@style/BaseAppTheme">
    </style>

</resources>
